name: Build and Push Synology Compiler 7.3

on:
  workflow_dispatch:
    inputs:
      push_to_hub:
        description: 'Push to Docker Hub'
        required: false
        default: 'true'
        type: boolean

env:
  REGISTRY: docker.io
  REPOSITORY: dante90/syno-compiler
  TOOLCHAIN_VERSION: 7.3
  PLATFORMS_K5: "epyc7002:5.10.55 geminilakenk:5.10.55 r1000nk:5.10.55 v1000nk:5.10.55"
  PLATFORMS_K4: "apollolake:4.4.302 broadwell:4.4.302 broadwellnk:4.4.302 broadwellnkv2:4.4.302 broadwellntbap:4.4.302 denverton:4.4.302 geminilake:4.4.302 kvmx64:4.4.302 purley:4.4.302 r1000:4.4.302 v1000:4.4.302"

jobs:
  # ==========================================================
  # Job 1: Build kernel 5.10 platforms (4 platforms)
  # ==========================================================
  build-k5:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      contents: read

    steps:
      - name: Free up disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
          sudo apt-get clean
          df -h /

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        if: github.event.inputs.push_to_hub == 'true'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Create opt/cache directory
        run: mkdir -p opt/cache

      - name: Download kernel 5.10 toolkits
        run: |
          set -e
          TOOLCHAIN_VERSION="${{ env.TOOLCHAIN_VERSION }}"
          BASE_URL="https://global.synologydownload.com/download/ToolChain/toolkit/${TOOLCHAIN_VERSION}"
          MAX_RETRIES=3
          TIMEOUT=30

          download_file() {
            local url=$1 output=$2 filename=$(basename "$output") attempt=1
            while [ $attempt -le $MAX_RETRIES ]; do
              echo "  [$attempt/${MAX_RETRIES}] Downloading ${filename}..."
              if curl -f -L --max-time $TIMEOUT -o "$output" "$url"; then
                echo "  ✓ Downloaded $(du -h "$output" | cut -f1)"
                return 0
              fi
              [ $attempt -lt $MAX_RETRIES ] && echo "  ⚠ Attempt $attempt failed, retrying..." && sleep 2
              ((attempt++))
            done
            echo "  ✗ Failed after ${MAX_RETRIES} attempts"
            return 1
          }

          echo "=== Downloading kernel 5.10 platforms ==="
          for V in ${{ env.PLATFORMS_K5 }}; do
            IFS=':' read -r PLATFORM KVER <<< "$V"
            echo "Processing platform: $PLATFORM (kernel $KVER)"
            DEV_FILENAME="ds.${PLATFORM}-${TOOLCHAIN_VERSION}.dev.txz"
            if ! download_file "${BASE_URL}/${PLATFORM}/${DEV_FILENAME}" "opt/cache/${DEV_FILENAME}"; then
              echo "  ✗ Failed to download ${DEV_FILENAME}"
              exit 1
            fi
          done

          echo ""
          echo "Downloaded files:"
          ls -lh opt/cache/

      - name: Build and push image (kernel 5.10)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: ${{ github.event.inputs.push_to_hub == 'true' }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.REPOSITORY }}:${{ env.TOOLCHAIN_VERSION }}
            ${{ env.REGISTRY }}/${{ env.REPOSITORY }}:latest
          build-args: |
            PLATFORMS=${{ env.PLATFORMS_K5 }}
            TOOLKIT_VER=${{ env.TOOLCHAIN_VERSION }}
          no-cache: true

      - name: Cleanup
        if: always()
        run: rm -rf opt/cache

  # ==========================================================
  # Job 2: Extend with kernel 4.4 platforms (11 platforms)
  # Uses Dockerfile.extend to build on top of 7.3-base image.
  #
  # To enable:
  #   1. Change build-k5 tags to push as 7.3-base (remove 7.3 and latest)
  #   2. Uncomment the build-k4 job below
  # ==========================================================
  # build-k4:
  #   runs-on: ubuntu-latest
  #   timeout-minutes: 120
  #   needs: build-k5
  #   permissions:
  #     contents: read
  #
  #   steps:
  #     - name: Free up disk space
  #       run: |
  #         sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache
  #         sudo apt-get clean
  #         df -h /
  #
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3
  #
  #     - name: Login to Docker Hub
  #       if: github.event.inputs.push_to_hub == 'true'
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKER_USERNAME }}
  #         password: ${{ secrets.DOCKER_PASSWORD }}
  #
  #     - name: Create opt/cache directory
  #       run: mkdir -p opt/cache
  #
  #     - name: Download kernel 4.4 toolkits
  #       run: |
  #         set -e
  #         TOOLCHAIN_VERSION="${{ env.TOOLCHAIN_VERSION }}"
  #         BASE_URL="https://global.synologydownload.com/download/ToolChain/toolkit/${TOOLCHAIN_VERSION}"
  #         MAX_RETRIES=3
  #         TIMEOUT=30
  #
  #         download_file() {
  #           local url=$1 output=$2 filename=$(basename "$output") attempt=1
  #           while [ $attempt -le $MAX_RETRIES ]; do
  #             echo "  [$attempt/${MAX_RETRIES}] Downloading ${filename}..."
  #             if curl -f -L --max-time $TIMEOUT -o "$output" "$url"; then
  #               echo "  ✓ Downloaded $(du -h "$output" | cut -f1)"
  #               return 0
  #             fi
  #             [ $attempt -lt $MAX_RETRIES ] && echo "  ⚠ Attempt $attempt failed, retrying..." && sleep 2
  #             ((attempt++))
  #           done
  #           echo "  ✗ Failed after ${MAX_RETRIES} attempts"
  #           return 1
  #         }
  #
  #         echo "=== Downloading kernel 4.4 platforms ==="
  #         for V in ${{ env.PLATFORMS_K4 }}; do
  #           IFS=':' read -r PLATFORM KVER <<< "$V"
  #           echo "Processing platform: $PLATFORM (kernel $KVER)"
  #           DEV_FILENAME="ds.${PLATFORM}-${TOOLCHAIN_VERSION}.dev.txz"
  #           if ! download_file "${BASE_URL}/${PLATFORM}/${DEV_FILENAME}" "opt/cache/${DEV_FILENAME}"; then
  #             echo "  ✗ Failed to download ${DEV_FILENAME}"
  #             exit 1
  #           fi
  #         done
  #
  #         echo ""
  #         echo "Downloaded files:"
  #         ls -lh opt/cache/
  #
  #     - name: Build and push final image (kernel 4.4 + 5.10)
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: ./Dockerfile.extend
  #         push: ${{ github.event.inputs.push_to_hub == 'true' }}
  #         tags: |
  #           ${{ env.REGISTRY }}/${{ env.REPOSITORY }}:${{ env.TOOLCHAIN_VERSION }}
  #           ${{ env.REGISTRY }}/${{ env.REPOSITORY }}:latest
  #         build-args: |
  #           BASE_IMAGE=${{ env.REGISTRY }}/${{ env.REPOSITORY }}:${{ env.TOOLCHAIN_VERSION }}-base
  #           PLATFORMS=${{ env.PLATFORMS_K4 }}
  #           TOOLKIT_VER=${{ env.TOOLCHAIN_VERSION }}
  #         no-cache: true
  #
  #     - name: Cleanup
  #       if: always()
  #       run: rm -rf opt/cache
